# gh — Harness on a cube chain (G + H)

Combine discrete Euclidean adjacency with constructive costed harness.

- Chain of cubes (face-gated transfer from G)
- Each cube may engage harness relative to its own `φ_ambient` (from H)
- Holding a non-ambient packet on a cube costs `κ|φ − φ_ambient| dt` into that cube's dissipated account
- Transfers remain face-only interventions

```
E_obs = Σ (E_config + E_dissipated)_cube
```

Not natural harness. Not Minecraft. Audit must close.
